---
description: Mesh 改代码后的固定发布流水线：本地确认 → 提交 → tmesh → 验证 → 生产
alwaysApply: true
---

# Mesh 发布流水线（每次改完代码）

完成 `mesh/` 下需要上线的改动后，**默认按此顺序执行**，不要只改本地就结束：

1. **本地确认**
 - 相关 pytest / smoke（至少覆盖改动点）
 - 只读页不得触发 Preview / Ask / Embed / Publish
2. **提交代码**（用户已授权本流水线时直接 commit；否则先问一句）
 - 按仓库 git 提交规范；不把 `.env`、密钥、`app-deploy.tgz`、无关 reports 打进 commit
3. **上测试环境 tmesh（镜像路径，强制）**
 - 主机 `104.250.53.182:22341`，目录 `/opt/geekpark-tmesh`
 - **正确：** `mesh/deploy/ship_image.ps1 -Target tmesh`
   - Git commit → `docker build` → `geekpark-mesh:<date>-<sha>` → digest → compose recreate
 - **禁止**把 `docker cp` 进运行中容器当作 Agent/Runtime 发布方式（recreate 丢代码）
 - 冒烟：路由非 404；核对 `ENV_REPRO_BASELINE.tmesh.json` 的 commit/digest
 - 入口：`https://tmesh.geekpark.net`
 - 规范：`mesh/docs/ENVIRONMENT_REPRODUCIBILITY.md`
4. **tmesh 没问题后上生产**
 - `ship_image.ps1 -Target prod`；目录 `/opt/geekpark-mesh`
 - **禁止**在未验证 tmesh 时直接上 prod
 - **不要**顺手 Publish / Preview / 跑全量 Ask，除非用户明确要求
 - 入口：`https://mesh.geekpark.ai`
5. **收尾**
 - 回报：commit、image tag/digest、tmesh 验证、prod 验证、可点开的 URL

遗留：`ship_changed_files.ps1` 仅紧急热修；热修后必须再用 `ship_image.ps1` 收口。

例外：纯文档/本地-only、用户明确说「先别上线」时跳过部署。
