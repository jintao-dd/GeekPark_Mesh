# GeekPark Mesh · 后台流程对照与验收 v1.3

> 本文档是《GeekPark Mesh · 产品与技术交接文档 v1.0》的**第 15 章补充**，请与交接包一起阅读。
> 交接人：珊珊（内容中心 / 品牌创意团队，系统管理员）
> 日期：2026-08-19（v1.3：沟通情报规范定案，阻塞项清空）
> 配套文件：`GeekPark_Mesh_周报后台_v0.8.html`（后台设计稿，浏览器直接打开）

---

## 0. 这份文档要解决什么问题

交接包里有**两个"后台"**，它们不冲突，但如果不说清楚，开发一定会只看其中一个：

| | 是什么 | 权威性 |
|---|---|---|
| `06_代码包/mesh/` | 能真正跑起来的 FastAPI 后台，八步流程 | **功能与数据结构以它为准** |
| `GeekPark_Mesh_周报后台_v0.8.html` | 单文件设计稿，四步流程 | **交互、呈现顺序、信任装置以它为准** |

**最容易出的事故**：开发拿到能跑的代码，照着代码实现界面，把设计稿当成"参考图"跳过。那样丢掉的不是美观，是这套系统赖以运转的信任装置——各部门愿不愿意交数据，取决于他们在后台里看到系统怎么对待他们交的东西。

**因此本章的核心是第 3 节的表**：设计稿里那 24 条管线步骤，不是加载动画，每一条对应一个后端必须真的执行的动作，且都有验收方法。**UI 上出现的每一条，后端必须真的做**；做不到的宁可从 UI 上删掉，也不许留在界面上假装做了。

---

## 1. 两套流程的对齐

### 1.1 对照表

| 代码包（八步，实现口径） | 设计稿（四步，呈现口径） | 结论 |
|---|---|---|
| ① 建期（slug / 起止日期 / 期次标签 / 版本号） | 无 | **代码对**。设计稿从"已建好本期"开始画。建期屏按代码包现状实现即可，不需要新设计。 |
| ② 上传 / 粘贴 / 抓取 RSS | 第一步 · 放入素材 | **合并为一屏**。设计稿的拖拽区 + 链接粘贴要保留；RSS 抓取按钮并入同屏，不另开一步。 |
| ③ 按类型 AI 抽取 | 第二步 · 挖掘 & 脱敏 | **设计稿对**。代码内部分几个函数无所谓，但**界面上必须呈现为一条连续管线**，见第 3 节。 |
| ④ 生成各团队要点卡 | （同上，管线内 `compose` 一步） | 同上，不得在 UI 上拆成两次点击。 |
| ⑤ 团队负责人放行 | 第三步 · 审校（`pass{}` / `held{}`） | 同一件事，字段口径见 1.3。 |
| ⑥ 生成周报草稿 | （管线内 `draft-save`） | 代码可保留为独立接口，UI 不单独成步。 |
| ⑦ 预览页双击编辑写回并留版本 | 第三步 · 审校内的编辑 / 移除 | 一致。双击写回保留。 |
| ⑧ 发布（禁用词检查）+ EDM | 第四步 · 确认上线 | **设计稿对且代码有缺口**，见 1.2。 |

### 1.2 代码包必须补的三个闸门

这三项设计稿有、代码包没有，**属于必做，不是优化**：

1. **角色门**。管理员可以一路做到第四步，但**上线按钮只对所有者显示**；管理员看到的是"提交给所有者确认"。代码包现有四种角色（admin / editor / dept / viewer）需增加 `owner`，且发布接口对非 owner 直接拒绝——不能只靠前端隐藏按钮。
2. **手打二字确认**。点"确认上线"后弹窗，必须在输入框手打"上线"两个字，按钮才可用。理由：上线不可撤回，且一旦发出全部收件人可见。
3. **版本不可改 + 归档**。上线生成 v1 并写入归档，记录：时间、由谁确认、几张卡对外、人工修订几张、对外 URL。上线后要改，**另发新版本，不覆盖旧的**。

### 1.3 放行的字段口径

设计稿用两个对象，代码包实现时请对齐：

- `pass{card_id: true}` —— 该部门负责人已放行，可对外。
- `held{card_id: true}` —— **暂扣**。暂扣不等于删除：卡片仍在库里、仍可追溯，只是本期不对外。
- 未放行 = 既不在 `pass` 也不在 `held` —— **默认不发**。这是保守默认，不许反过来。

上线前检查清单（`chk`）里，"所有卡均已放行或暂扣"是**硬条件**（非 soft），未满足时上线按钮禁用。

---

## 2. 四屏交互规格

### 第一步 · 放入素材（`s1`）

- 拖拽区 + 点击选择。接受文档 `txt / md / docx / pdf / csv / ics`；录音 `mp3 / m4a / wav / aac / flac`；录屏 `mp4 / mov`。其余类型在区内直接给出拒绝提示，不静默失败。
- 每个文件一行，显示文件名 + 归入路径 + 类型标签。
- **部门归属下拉**：九个部门（编辑部 / 商业化团队 / 硅谷 BD 团队 / Global Partnership 团队 / 品牌创意团队 / 投资团队 / 社区团队 / 视频团队 / 播客团队）+ 一个特殊项**内容中心·数据聚合**。
  - 选中"内容中心·数据聚合"时提示：*识别为内容中心跨源聚合，不归任何部门*。**它是来源不是部门，不进部门计数。**
  - 自动识别到的显示"自动识别，可改"；手动指定显示"已手动指定"；未选显示"先不选也行，处理时会再判断"——**不许把未选做成阻塞项**。
- 链接粘贴区（已发布文章、RSS 等）。
- **悬浮说明入口**：原"承诺屏"的全部内容收进这里（系统会怎么对待你交的材料、⑤区不进抽取、原文留在隔离区、脱敏只在分发层）。入口只放在本屏——最贴近"交东西那一刻"。承诺屏已整个撤销，不要复活它。
- 「下一步：挖掘 & 脱敏」→ 进入第二屏并**自动开跑**（延迟约 420ms）。

### 第二步 · 挖掘 & 脱敏（`s3`）

- **进屏即自动运行，不要"先列步骤再点按钮开始"。** `#gen` 按钮默认隐藏，仅作为失败后的重试入口。
- 两层展示：
  - **文件处理层**（3 条，可折叠 `<details>`，运行时自动展开）：接收·隔离入库 / 转写与识别 / 清洗·去重。
  - **设计层**（21 条，`.dlist` 滚动容器 `max-height:224px`，上下渐隐遮罩）：当前步 `scrollIntoView({block:'center'})`，**同时只露几条**。
- 每条显示：中文步骤名 + 英文 `sk` 短码 + 一句人话解释 + 右侧结果数。
- 「转写与识别」带 `warn` 态（有录音文件时）：**说话人归属可能不准，转写稿只进原始层**。这条不许去掉。
- 跑完显示结果摘要 → 「下一步：审校」。

### 第三步 · 审校（`s4`）

- 卡片按五块分组呈现：可同步的关系 / 接触过的人和公司 / 关注了什么 / 日程与计划 / 沟通中提到的看法。
- 每张卡可编辑、可移除、可放行、可暂扣。
- 被 ⑤ 区硬拦的条目**红底显示、可核对**，但不可一键放行——需管理员显式操作并留痕。
- 人工修订过的卡打内部标记（`edited`），只在后台可见，读者页不显示。
- 「预览读者页」按钮：在新窗口打开真实读者视图，不是缩略图。

### 第四步 · 确认上线（`s5`）

- 左侧：上线前检查清单（`chk`），硬条件全绿才允许上线；soft 项仅提示。
- 右侧：角色门 + 版本记录。见 1.2。
- 上线后本屏文案改为：*本期已上线。要改动请在审校页修改后另发新版本（v2），旧版本保留在归档。*

---

## 3. 25 步管线 → 后端硬性验收条件

**这是本文档最重要的一节。** 左列是设计稿 UI 上会走过的步骤（`sk` 为稳定短码，代码里请用同名标识），右列是后端必须真的做到的事。验收时逐条走，做不到的当场提出，不许悄悄跳过。

### 3.1 文件处理层（3 条）

| `sk` | 步骤 | 后端必须做到 |
|---|---|---|
| `intake` | 接收 · 隔离入库 | 原始文件**原样**写入原始层，与对外网站**物理隔离**（不同存储路径/桶，Web 进程无读权限）。验收：从公网无法通过任何 URL 拼接访问到原始文件。 |
| `transcribe` | 转写与识别 | 录音转文字、录屏抽帧拼接、按说话人分段。**转写稿只进原始层，不直接进抽取**。有录音时 UI 必须显示说话人归属可能不准的警告。 |
| `normalize` | 清洗 · 去重 | 去寒暄、去重复、统一格式。 |

### 3.2 设计层（21 条）

| `sk` | 步骤 | 后端必须做到 / 验收方法 |
|---|---|---|
| `extract` | 按类型分别抽取 | **每类材料一套规则，不混为一谈**：例会分"决定"与"想法"；沟通记录用真名；周报按白名单字段；文章取标题/日期/作者/链接。验收：`app/prompts/` 下每类材料有独立提示词文件。 |
| `owner-attrib` | 归属判定 · 条目级 | **按条目判定归属团队，不按文件。** 部门直接提交的文档整份继承该部门；聚合器抓来的混合文档逐条拆分归属。每个抽取条目必须带 `owner_team` 与 `channel` 两个字段。验收：拿一份含多部门内容的聚合文档，确认条目分别落到正确团队。 |
| `zone5-filter` | ⑤区过滤 · 人与钱 | 融资金额、条款、谈判立场、薪酬**命中即整条拦截，代码硬执行**（不依赖模型自觉）。原文全量留隔离区，只在对外层暂扣。验收：构造含金额的测试样本，确认被拦且原文可追溯。 |
| `listed-check` | 上市 / pre-IPO 识别 | 涉及上市或临近上市公司，给投资侧收件人版本单独加过滤。 |
| `pool-match` | 跨期比对 · 系统的记忆 | 对照信息池**近 12 期**，识别首次进入 / 与往期同名 / 两处说法待核对。**没有这一步周报就只是一次性摘要**。验收：第二期起必须能标出首次与非首次。 |
| `cross-channel-merge` | 跨通道合并 · 同一件事只算一次 | 同一场会可能同时出现在日历、会议录音与聚合文档中。**按「实体 + 事件」合并，不按文件去重**；合并后**保留全部来源行**。**必须在 `relation-link` 之前执行**，否则同一团队的数据到达两次会被误判为跨部门关系。验收：构造同一事件三通道到达的样本，确认合并为一张卡且带三行来源。 |
| `relation-link` | 关系匹配 | 判定谁接触了、谁在关注、谁已联动，用平实标签，**不做对抗性描述**。 |
| `sync-pick` | 可同步性判断 | **只看 `owner_team` 是否不同，不看来源记录条数。** 同一团队多通道重叠已在上一步合并；只有不同团队各自碰到同一人或事，才进"可同步的关系"。**这是 Mesh 与信息墙的分界，也是本产品最容易出假阳性的地方。** |
| `compose` | 五块合成 | 可同步的关系 · 接触过的人和公司 · 关注了什么 · 日程与计划 · 沟通中提到的看法。 |
| `cite` | 来源行生成 | 内部来源写到**会议级、不带时间戳**（如"编辑部沟通记录"）；公开来源写标题+日期+作者+链接。**每卡至少一行来源**。验收：无来源的卡不允许进草稿。 |
| `lint` | 术语与标签检查 | 禁用词（共振 / 同现 / 主体 / × / 不一致 / 冲突 / 缺口 / 看法不同 / 分歧）、日程确定度齐全、"不代表承诺"提示存在、只列本期提交团队。**发布接口再查一次，不只在此处查**。 |
| `name-verify` | 姓名与职级核对 | **对话主体保留真名**——人名、公司、职级都留。只有金额、条款、谈判立场、薪酬这四类才锁。核对拼写与职级。 |
| `zone-sort` | 六区分拣 | 每段归入：可公开 / 需脱敏 / 仅内部 / 待核对 / ⑤区人与钱 / 无关噪声。分区决定后续处理。 |
| `decision-split` | 决定与想法分离 | "定了要做"与"随口一提"必须分开。**拿不准一律降级为想法**，不让探索性发言变成隐性任务。 |
| `speaker-doubt` | 说话人存疑标记 | 靠语音识别分的段，不确定是谁说的就**不写进带姓名的卡**，或标注待人工确认。 |
| `value-extract` | 价值提取 · 有没有下一步 | 看有没有可展开的下一步，**不是热闹就留**。没有下一步的闲聊只存档不成卡。 |
| `certainty-tag` | 确定度标注 | 日程每条打：已排期 / 在做 / 计划中 / 在谈 / 初步想法 / 日历订阅 / 外部事件。**初步想法用虚线框**，配"不代表承诺、不构成任务"提示。 |
| `first-seen` | 首次进入判定 | 确认哪些人和公司本期第一次进入记录。**本周导语从这里取角度，每期不同，只讲事实**。 |
| `invest-guard` | 投资侧合规二次过滤 | 给投资侧收件人的版本再过一遍：上市与临近上市公司只留"已接触"，隐去对方表述与进展。 |
| `contrib-list` | 贡献团队清点 | **按 `owner_team` 计，不按 `channel` 计。** 聚合器抓来的内容也要落到它真正归属的团队。只列本期有材料进来的团队，**不写谁没交**。 |
| `trace-keep` | 可追溯留痕 | 被暂扣和被脱敏的原文**全量留隔离区**，附来源与时间。**脱敏只发生在对外这一层，底库不动**。 |
| `draft-save` | 落盘为草稿 | 生成 `draft-1` 进审校台，**对读者不可见**。 |

---

## 4. v0.7 → v0.8 修复记录

上传给我的 v0.7 有三处 bug，v0.8 已修，开发请**直接使用 v0.8**：

1. `const SCREEN=[0,3,4,5]` → `[1,3,4,5]`。原值把第一步指向不存在的 `#s0`，挖掘屏的「← 返回」会抛空引用异常。
2. 脚本结尾 `renderDots();render();` → 追加 `go(0);`。原先载入时没有任何 `.screen` 带 `.on` 类，而 `.screen{display:none}`，**整页空白**。
3. `reach()` 中 `Math.max(r,0)` 是空操作，且 `generated` 直接跳到 3 越过审校。改为：有文件→1，已生成→2，进过审校→3。

已通过 `node --check` 语法校验。

---

## 5. 不许自行简化的清单

开发若认为以下任一项"可以先不做"，请先问管理员，不要自行决定：

1. 24 步管线的**任何一条**（要么后端真做，要么 UI 上删掉，不许留着假装）。
2. 上线的三道闸门：角色门、手打"上线"二字、版本不可改。
3. 未放行默认不发。
4. ⑤区硬拦由代码执行，不依赖模型判断。
5. 原始层与对外层的物理隔离。
6. 每卡至少一行来源。
7. 只列本期提交团队，不写谁没交。
8. "初步想法"虚线框与"不代表承诺、不构成任务"提示。
9. 承诺屏内容收进悬浮入口——不要复活承诺屏，也不要把它删干净。

---

## 6. 基础设施与已决事项（2026-08-19 管理员拍板）

### 6.1 托管方案：新加坡节点 + `.ai` 域名 + 模型待定

- **部署地**：新加坡节点。`raw-storage` 层可以开工。
- **域名**：`mesh.geekpark.ai`，不变。
- **模型**：**未定**，可能是境外模型（Anthropic 等）或境内模型（豆包等）。

模型未定不阻塞开发，但**必须按下面的方式写，否则换模型时要返工**：

**`model-call` 必须做成适配层，不许把某一家的 SDK 调用散落在业务代码里。**

- 定义一个统一接口（如 `app/llm/base.py`），暴露 `complete(prompt, system, max_tokens) -> str` 与 `complete_json(...) -> dict` 两个方法。
- 每家模型一个实现文件（`anthropic.py` / `doubao.py` / …），由 `.env` 里一个 `MESH_LLM_PROVIDER` 变量切换。
- 业务代码（抽取、生成草稿、AI 问答）**只依赖接口，不 import 任何厂商 SDK**。
- 验收方法：把 provider 从 A 切到 B，业务代码零改动即可跑通。

换模型时必须重新验证的三件事（写进上线检查）：

1. **抽取质量**——各类材料的提示词在新模型上要重跑一遍样本，`app/prompts/` 里的规则可能要按新模型的脾气调。
2. **上下文窗口**——不同模型窗口差异大，切分策略不许写死"一次塞完"，必须按 token 数分块并可配置。
3. **脱敏判断**——凡是依赖模型语义判断的环节（如六区分拣、决定与想法分离）都要重测。
   **注意：⑤区硬拦不在此列**——它是代码正则/规则硬执行，不依赖模型，换模型不影响。这正是当初坚持"硬执行"的价值。

**两个需要你找法务确认的点（我不是律师，只提示风险，不做判断）：**

- **数据出境**。系统存有含真实人名、公司、职级的内部沟通记录。境内出到新加坡节点一事，管理员已确认无问题（见 6.5）。
- **若选豆包等境内模型**，服务在境内，等于数据从新加坡再回流境内，形成一次二次跨境；同时要确认厂商侧的保密条款是否允许承载这类内部记录。

### 6.2 海外社媒（T10）归属：硅谷 BD 团队

英文站与海外社媒账号数据归**硅谷 BD 团队**。放入素材屏的部门下拉默认指向该团队。

### 6.3 硅谷 BD 三类数据：定时抓取，经内容中心·数据聚合

日程 / 日历、外联记录、LinkedIn / X 动态**已有固定导出**，由内容中心·数据聚合定时抓取，不走人工上传。

- 接入方式：定时任务，不占用放入素材屏的人工流程。
- 抓取失败要在后台可见并可重试，**不许静默跳过**——这是最高优先级数据源。

> ⚠️ **6.2 与 6.3 存在口径冲突，见 6.4，未解决前不要实现贡献团队清点。**

### 6.4 双通道归属模型（已定案）

数据有**两条进入路径**，两条都可能覆盖同一个团队、同一件事：

| 通道 `channel` | 说明 |
|---|---|
| `aggregator` | 内容聚合器（自研工具）自动抓取，生成内容聚合文档 |
| `manual` | 部门自行提交给总裁办，由管理员（总裁办）手动整理后上传 |

**三层模型，三者互不混用：**

1. **通道 `channel`** —— 数据怎么来的。只做留痕，**不参与任何判断**。
2. **归属 `owner_team`** —— 这条数据属于哪个团队。**粒度是条目，不是文件。**
   - 部门直接提交的文档：整份继承该部门。
   - 聚合器抓来的文档：**是混合体**，一份里可能同时含硅谷 BD、编辑部、商业化的内容，必须逐条拆分归属。
   - 「内容中心·数据聚合」**不再作为归属值**，它是通道不是团队。放入素材屏的下拉里保留该选项，但含义改为"混合文档，按条归属"。
3. **合并 `merge`** —— 跨通道去重按「实体 + 事件」做，不按文件做；合并后保留全部来源行。

**举例（硅谷 BD 团队）：** Lilyann 团队同一场会面，可能同时出现在行动日历（`aggregator`）、会议录音（`manual`）、聚合文档（`aggregator`）中。三条 `owner_team` 都是硅谷 BD 团队，`cross-channel-merge` 将其合并为一张卡，卡上带三行来源。贡献团队清点计入硅谷 BD 团队一次。

**为什么这一节是硬要求：**

- 不做条目级归属 → 硅谷 BD（最高优先级来源）永远不出现在贡献团队里，最该被看见的团队反而隐形。
- 不做跨通道合并 → 同一团队数据到达两次被误判为"多处记录"，塞进「可同步的关系」。**那是假阳性，而且读者看不出来是错的**，只会认为系统在瞎报，进而不再信任它。可同步的关系是本产品的核心块，它的价值前提是"**不止一个部门**碰到了同一个人或事"。

**重叠是常态，不是异常。** 两条通道本就会拿到同一批信息，合并逻辑必须按常态设计，不许当成错误抛异常或让管理员手工挑。

### 6.5 法务确认结论

新加坡节点的数据出境问题，管理员已确认**无问题**（2026-08-19）。若最终选用境内模型（豆包等）造成二次跨境，请就该场景再确认一次。

### 6.6 沟通情报规范：已重写为 v1.0

《周报规范-沟通情报 v0.5 草稿》**已作废**，由《周报规范-沟通情报 v1.0》取代。

作废理由：v0.5 写于 2026-08-11，早于五块结构、实名规则、双通道归属的定案，其价值信号矩阵与隐名规则与现行规则正面冲突，无法在其基础上修订。

v1.0 相对 v0.5 的实质变更（开发请按 v1.0 实现，**不要参考 v0.5**）：

| 项 | v0.5 | v1.0 |
|---|---|---|
| 姓名处理 | L1 隐名，职务泛化到层级 | **一律实名**，只锁金额/条款/谈判立场/薪酬四类 |
| 受众分发 | 六信号 × 六受众矩阵，分受众判断价值 | **废除**。同一份周报给所有人，唯一例外是投资侧合规过滤 |
| 输出结构 | L1 日报 + L2 七模块 | **对齐 Mesh 五块** |
| 通道 | 未涉及 | 双通道 `aggregator` + `manual`，条目级归属，跨通道合并 |
| 填报人 | 显示"采编·XX"，可单条匿名 | **只到团队级**，不显示个人 |
| L3 解禁 | 到期或官宣后自动进入下期 | **改为人工确认**，不自动放行 |
| 可信度 | 有定义 | 保留并要求随卡呈现（一手/二手 · 有无原始记录 · 单源/多源）|

**交接包操作**：`02_规范/` 中删除 `周报规范-沟通情报_v0.5草稿.md`，放入 `周报规范-沟通情报_v1.0.md`。

### 6.7 阻塞项状态

**全部清空。** 托管（新加坡 + `.ai`）、海外社媒归属（硅谷 BD）、硅谷 BD 数据形式（定时抓取）、双通道归属模型、法务出境、沟通情报规范——均已定案。

模型选型（境外 / 豆包等）仍未定，但已按 6.1 的适配层写法处理，**不阻塞开发**。
